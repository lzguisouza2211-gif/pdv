node_modules
dist
electron-dist
release
.env
.env.local
.env.*.local
*.log
.DS_Store
# Credenciais geradas — nunca commitar
electron/main/credentials.ts
electron/main/credentials.js

# Sessão WhatsApp — dados sensíveis, nunca commitar
auth_info_baileys/
**/auth_info_baileys/

credentials.ts

# estudos
mind

#Danger
reset_database.sql